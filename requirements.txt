fastapi==0.111.0 # 最新稳定版
uvicorn==0.29.0 # 最新稳定版
pyodbc==5.1.0 # 确保与您的 ODBC 驱动兼容，根据实际情况选择
pydantic==2.7.1 # Pydantic v2
pydantic-settings==2.2.1 # 用于配置管理
python-dotenv==1.0.1 # 用于加载 .env 文件

# 测试依赖
pytest==8.2.1
pytest-asyncio==0.23.0
httpx==0.27.0 # 用于 API 集成测试 